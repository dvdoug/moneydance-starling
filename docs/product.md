# Product

Import-only Starling → Moneydance. Same confirm/merge path as other downloads.

Spaces are optional mapping rows. Default is statement-shaped current account. Easy Saver is a separate Starling account. See `docs/user/`.
